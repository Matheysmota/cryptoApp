<resources xmlns:tools="http://schemas.android.com/tools" xmlns:name="http://schemas.android.com/apk/res-auto">
    <!-- Base application theme. -->
    <style name="Theme.CryptoApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:itemTextAppearance">@color/white</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@color/app_bar</item>
        <item name="colorPrimary">@color/app_bar</item>
        <item name="colorPrimaryVariant">@color/app_bar</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.LoginCryptoApp" parent="Theme.CryptoApp">
        <!-- Primary brand color. -->
        <item name="android:windowTranslucentStatus">true</item>
    </style>
    <style name="ThemeMyDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Primary brand color. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
    <style name="mySubTittle" parent="android:TextAppearance.DeviceDefault">
        <!-- Primary brand color. -->
        <item name="android:textColor">@color/white</item>
    </style>
</resources>